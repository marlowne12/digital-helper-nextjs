# Launch Checklist — Operator-Grade Finish + Local SEO

Generated by the redesign workflow on branch `redesign/operator-grade-finish`.
Stages: audit → brand-consistency → SEO/technical → build-fix → QA → security/launch.

---

## ✅ Completed in this pass (verified)

- [x] **Build passes clean** — `npm run build` exits 0, 91/91 static pages generated. All
      dependencies declared in `package.json` (added `@notionhq/client`, `@vercel/kv`, `resend`).
- [x] **AI SDK v6 migration (full stack)** — `aiTools.ts` (`parameters`→`inputSchema`),
      `api/chat/route.ts` (`convertToModelMessages` + `toUIMessageStreamResponse`), removed stale
      `@ts-expect-error`, AND `ChatWidget.tsx` migrated to UIMessage `parts` (Codex review caught
      the client was half-migrated — would have 500'd the first chat / rendered blank replies).
- [x] **Lint clean** — 0 errors (12 cosmetic unused-var warnings remain, non-blocking).
- [x] **Tests green** — 13/13 (`vitest run`).
- [x] **Single NAP source of truth** — `layout.tsx` and `StructuredData.tsx` now read all
      Name/Address/Phone from `src/lib/business-info.ts`. No more conflicting schemas.
- [x] **Sitemap** — full canonical route set + dynamic blog posts/categories (`src/app/sitemap.ts`).
- [x] **robots.ts** — disallows `/api/` and `/admin/`; points to sitemap.
- [x] **Canonicalization** — 301 redirects for thin `/locations/{city}` → richer `/{city}-wa`,
      plus the pre-existing `/services/*` → short-route redirects (`next.config.ts`).

---

## 🔴 Human decisions / access required before deploy

- [ ] **Confirm `business@digital-helper.com` is a live, monitored mailbox.** Canonical email is
      now `business@digital-helper.com` everywhere (NAP + schema + chatbot persona). The contact
      and lead-magnet API routes send via Resend — verify deliverability or this silently drops leads.
- [ ] **Vercel environment variables** (server-side only — never client):
      - `GEMINI_API_KEY` / `GOOGLE_GENERATIVE_AI_API_KEY` (chat, audit, content gen)
      - `NOTION_API_KEY` + database id (contact → Notion CRM)
      - `RESEND_API_KEY` (transactional email)
      - `KV_REST_API_URL` / `KV_REST_API_TOKEN` (Vercel KV — leads storage)
      - Upstash Redis creds (rate limiting): `UPSTASH_REDIS_REST_URL` / `_TOKEN`
      - Supabase URL + anon/service keys
      - Admin auth secret (`/admin` login)
      Run `grep -rn "process.env" src/ | sort -u` for the authoritative list before launch.
- [ ] **Real business address (optional, ranking upside).** Currently a service-area business
      (areaServed only, no street). If a verifiable address exists, add it to `BUSINESS_INFO.address`
      and the `LocalBusiness` schema to strengthen local-pack eligibility.
- [ ] **`og-image.png` + `logo.png`** — confirm these exist in `/public` and reflect the new brand.
- [ ] **`aggregateRating` in schema is hardcoded 4.9/50 reviews.** If those aren't real, remove
      the `aggregateRating` block (fake review schema risks a Google manual action).

---

## 🟡 Recommended before / shortly after launch

- [ ] **Lighthouse mobile** on home + top 5 (web-design, seo, ai-agency, richland-wa, pricing).
      Targets: Performance ≥ 90, SEO ≥ 95, Accessibility ≥ 90.
- [ ] **Rich Results test** the JSON-LD (LocalBusiness, Service, FAQ, Breadcrumb) at
      https://search.google.com/test/rich-results after deploy.
- [ ] **Submit sitemap** in Google Search Console (`https://digital-helper.com/sitemap.xml`).
- [ ] **Lead-path smoke test** in production: contact form → Notion; `/audit` tool end-to-end.
- [ ] Replace the `middleware` file with `proxy` (Next 16 deprecation warning, non-blocking).
- [ ] Clear the 12 lint warnings (`npm run lint -- --fix` handles most).

---

## Notes on decisions made autonomously (review if you disagree)

- **Location canonical = `/{city}-wa`** (richer, keyword-targeted, 148L) over the thin 47L
  `/locations/{city}` duplicates. West Richland has no `-wa` twin, so `/locations/west-richland`
  stays canonical — consider creating `/west-richland-wa` for symmetry.
- **`/work` kept alongside `/case-studies`** (only `/work/case-studies` was redirected). Confirm
  whether `/work` should also redirect to `/case-studies` or serve a distinct portfolio page.
